import static java.lang.Math.*;
import static java.lang.IO.*;

void main() {
    var d = random();     // 不需要加类名前缀
    var pi = PI;
    println("d = " + d);    // out是System类的一个静态成员
    println("pi = " + pi);
}